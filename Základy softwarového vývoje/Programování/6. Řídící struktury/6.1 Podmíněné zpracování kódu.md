
---

## 🧩 Příkaz `if` a `else`

### Základní podmínka (`if`)

Příkaz `if` je základní strukturou pro vykonání bloku kódu, **pouze pokud** je daná podmínka vyhodnocena jako **`true`** (_truthy_).

```javascript
if (podmínka) {
  // Kód se provede, pokud je podmínka truthy
}
```

> [!tip] Používání složených závorek {}
> 
> Vždy používejte složené závorky, i když je uvnitř if jen jeden příkaz. Zvyšuje to čitelnost a předchází chybám při přidávání dalšího kódu.

### Větev pro nepravdu (`else`)

Blok `else` se spustí, pokud podmínka v `if` je **`false`** (_falsy_).

```javascript
if (year == 2015) {
  alert("Správně!");
} else {
  alert("Špatně."); // Spustí se, pokud year není 2015
}
```

### Řetězení podmínek (`else if`)

Umožňuje testovat více vzájemně se vylučujících variant. Kód se vyhodnocuje shora dolů, a jakmile je jedna podmínka `true`, zbytek větví se ignoruje.

```javascript
if (year < 2015) {
  alert("Příliš brzy...");
} else if (year > 2015) {
  alert("Příliš pozdě");
} else {
  alert("Přesně!");
}
// Závěrečný else je volitelný.
```

---

## 🔍 Převod na Boolean (Truthy/Falsy)

Výraz uvnitř `if (...)` se vždy automaticky převede na boolean. Podmíněný kód se provede, pokud je výsledek **truthy**.

|**Hodnota**|**Výsledek v if**|
|---|---|
|**Falsy**|`0`, `""`, `null`, `undefined`, `NaN`, `false`|
|**Truthy**|Všechny ostatní hodnoty (včetně neprázdných řetězců, čísel ≠ 0, objektů, polí)|

Pro přehlednost je možné použít i předem vypočítanou boolean proměnnou:

```javascript
let cond = (year == 2015);
if (cond) { /* ... */ } 
```

---

## ❓ Podmíněný (Ternární) Operátor `?`

Ternární operátor je zkrácený zápis pro **výběr mezi dvěma hodnotami** na základě podmínky.

### Základní syntaxe

```javascript
let result = podmínka ? hodnota1 : hodnota2;
```

- Pokud je **podmínka** `true` (truthy), vrátí se `hodnota1`.
    
- Jinak se vrátí `hodnota2`.
 
> [!example] Příklad ternárního operátoru
> 
> **Lze zjednodušit:**
> 
> ```javascript
> // Původní:
> let accessAllowed = (age > 18) ? true : false;
>  // Zjednodušený (a preferovaný):
> 
> let accessAllowed = age > 18; // Vrátí přímo true/false
> ```

### 🔗 Vícenásobný ternární operátor

Lze jej řetězit, čímž nahrazuje více `if...else if` větví.

```javascript
let message = (age < 3) ? "Hi, baby!" :
              (age < 18) ? "Hello!" :
              (age < 100) ? "Greetings!" :
              "What an unusual age!";
```

### ⚠️ Upozornění (Netypické použití)

Nedoporučuje se používat ternární operátor `?` k vykonání různých příkazů (místo `if/else`):

```javascript
// Nedoporučeno, protože snižuje čitelnost
(company == 'Netscape') ? alert('Right!') : alert('Wrong.');
```

> [!tip] Použití
> 
> Používejte ? primárně pro přiřazení hodnot, zatímco if/else používejte pro větvení toku kódu (spouštění různých akcí/příkazů).