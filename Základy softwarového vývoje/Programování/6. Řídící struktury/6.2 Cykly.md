
---

## 🔄 Smyčka `while`

Smyčka **`while`** je nejjednodušší a provádí se, dokud je daná podmínka **truthy**.

### 🔑 Chování a syntaxe

- **Kontrola:** Podmínka se vyhodnocuje **před** každou iterací.
- **Syntaxe:**
    
    ```javascript
    while (podmínka) {
        // tělo smyčky
    }
    ```
    
- **Zkrácený zápis:** Pokud má tělo pouze jeden příkaz, lze vynechat složené závorky `{}`.
    
    ```javascript
    let i = 3;
    while (i) alert(i--); // 3, 2, 1, pak se zastaví (0 je falsy)
    ```

---

## 🔁 Smyčka `do...while`

Smyčka **`do...while`** se používá, když je potřeba zajistit, aby se kód provedl **alespoň jednou**.

### 🔑 Chování a syntaxe

- **Kontrola:** Tělo smyčky se provede nejdříve, podmínka se kontroluje **až poté**.
- **Syntaxe:**
    
    ```javascript
    do {
        // tělo smyčky (provede se minimálně jednou)
    } while (podmínka);
    ```

---

## ➿ Smyčka `for`

Smyčka **`for`** je nejpoužívanější a nejuniverzálnější, protože kombinuje řídící mechanismy do jedné hlavičky.

### 🔑 Chování a syntaxe

```javascript
for (begin; condition; step) {
    // tělo smyčky (body)
}
```

- **`begin` (Inicializace):** Provede se **jednou na začátku**.
- **`condition` (Podmínka):** Kontrola před každou iterací.
- **`body` (Tělo):** Kód, který se opakuje.
- **`step` (Krok):** Provede se po každém těle.

> [!tip] Inline deklarace
> 
> Proměnná deklarovaná v části begin (např. for (let i = 0; ...) ) je viditelná pouze uvnitř smyčky.

### ⚠️ Možnost vynechat části

Lze vynechat `begin`, `step` nebo obojí. **`for(;;)`** vytvoří **nekonečnou smyčku**.

---

## 🛑 `break` a `continue`

Tyto speciální příkazy slouží k řízení toku uvnitř smyčky.

|**Příkaz**|**Popis**|
|---|---|
|**`break`**|Okamžitě **ukončí** celou smyčku a pokračuje kódem za smyčkou.|
|**`continue`**|Okamžitě **přeskočí** zbytek kódu v aktuální iteraci a pokračuje **další** iterací (přechází na krok `step` u `for`, nebo kontrolu podmínky u `while`).|

> [!example] Příklad `continue`
> 
> Vypíše pouze lichá čísla:
> 
> ```javascript
> for (let i = 0; i < 10; i++) {
>     if (i % 2 === 0) continue; // Přeskočí sudá čísla
>     alert(i); // 1, 3, 5, 7, 9
> }
> ```

---

## 🏷 Štítky (_Labels_)

Štítky umožňují příkazům `break` a `continue` pracovat s více úrovněmi vnořených smyček, než jen s tou nejbližší.

### 🔑 Syntaxe a použití

- Umístěte štítek (např. `labelName:`) před vnější smyčku.
- Uvnitř vnořené smyčky použijte `break labelName` nebo `continue labelName`.

```javascript
labelName: for (let i = 0; i < 5; i++) {
    for (let j = 0; j < 5; j++) {
        if (j == 2) break labelName; // Ukončí i vnější smyčku!
    }
}
```

- **Omezení:** Nelze "skákat" na libovolné místo v kódu; štítek musí označovat smyčku.