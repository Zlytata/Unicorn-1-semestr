
---

### 🔑 Syntaxe a porovnávání

- **Syntaxe:** Skládá se z klíčového slova `switch(výraz)`, jednoho nebo více bloků `case` a nepovinného bloku `default`.
    
- **Porovnávání:** Hodnota ve výrazu `switch` se porovnává s hodnotami v každém `case` bloku pomocí **striktní rovnosti (`===`)**.

```javascript
let a = 2 + 2;

switch (a) {
    case 3:
        alert("Příliš málo");
        break;
    case 4:
        alert("Přesně tak!"); // Zde se kód spustí
        break;
    default:
        alert("Neznámá hodnota");
}
```

---

## 🎯 Řízení toku

### `break` a "Fall-through"

- **`break`**: Klíčové slovo `break` je **zásadní**. Ukončí provádění kódu a vyskočí z celého `switch` bloku.
- **"Fall-through" (Propádání):** Pokud `break` **chybí**, provádění kódu bude **pokračovat do dalšího `case` bloku**, a to bez ohledu na shodu, dokud nenarazí na `break` nebo konec `switch` příkazu.

### `default` a Seskupování

- **`default`**: Blok `default` je nepovinný. Provede se, pokud **žádný** z `case` bloků neodpovídá. Funguje jako `else` v `if` příkazu.
- **Seskupování (`case`):** Více `case` bloků může být seskupeno, aby sdílely stejný kód. Stačí je napsat pod sebe bez `break`.

> [!tip] Použití: **Používejte `switch`** pro porovnání jedné proměnné s pevnými hodnotami, protože je čitelnější. **Používejte `if...else if`** pro složitější logiku s dynamickými rozsahy nebo komplexními podmínkami.