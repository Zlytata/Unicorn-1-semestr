
---

## 📚 Základní pojmy a Typy operátorů

- **Operand** – Hodnota, na kterou se operátor aplikuje.
- **Unární operátor** – Pracuje s jedním operandem (např. `-x`).
- **Binární operátor** – Pracuje se dvěma operandy (např. `x - y`).
- **Ternární operátor** – Pracuje se třemi operandy (jediný v JS je `? :`).

---
## ➕ Aritmetické operátory

Používají se pro matematické výpočty.

| **Operátor** | **Popis**                   | **Příklad**   |
| ------------ | --------------------------- | ------------- |
| `+`          | Sčítání                     | `5 + 2 = 7`   |
| `-`          | Odčítání                    | `5 - 2 = 3`   |
| `*`          | Násobení                    | `5 * 2 = 10`  |
| `/`          | Dělení                      | `5 / 2 = 2.5` |
| `%`          | Zbytek po dělení (_Modulo_) | `5 % 2 = 1`   |
| `**`         | Umocnění                    | `2 ** 3 = 8`  |

### 🔗 Speciální chování operátoru `+` (Spojování řetězců)

> [!tip] Spojování (Konkatenace)
> 
> Pokud je alespoň jeden operand řetězec, operátor + provede spojování řetězců (string concatenation).
> 
> ```javascript
> alert("1" + 2); // "12"
> alert(2 + "1"); // "21"
> ```

> [!note] Ostatní operátory
> 
> Ostatní aritmetické operátory (-, *, / atd.) automaticky převádějí řetězce na čísla:
> 
> ```javascript
> alert("6" - "2"); // 4 (proběhla konverze na číslo)
> ```

### ➕ Unární plus

Unární operátor `+` (před operandem) nemá nic společného se sčítáním. **Slouží k rychlé konverzi operandu na číslo** (_Number_).

```javascript
alert(+true); // 1
alert(+"");   // 0
alert(+"123"); // 123
```

---

## 🟰 Operátory přiřazení

### Základní přiřazení (`=`)

- Operátor `=` slouží k přiřazení hodnoty do proměnné.
- **Vrací přiřazenou hodnotu**, což umožňuje řetězení přiřazení a vnořená přiřazení.

```javascript
let a = 1;
let b = 2;
// Vnořené přiřazení: (a = b + 1) nejdřív přiřadí 'a' hodnotu 3, 
// a pak vrátí tuto hodnotu pro odčítání: c = 3 - 3
let c = 3 - (a = b + 1); // a = 3, c = 0
```

### ✍️ Zkrácené zápisy (Kombinované přiřazení)

Kombinace aritmetického operátoru s přiřazením zkracuje zápis `n = n [operátor] hodnota`.

- `+=`, `*=`, `-=`, `/=`, `%=`, `**=` atd.

```javascript
let n = 2;
n += 5; // n = 2 + 5 = 7
n *= 2; // n = 7 * 2 = 14
```

---

## 🔼 Inkrementace a dekrementace

Zvyšují (`++`) nebo snižují (`--`) hodnotu proměnné o **jedna**. Lze je použít pouze na proměnné.

### Prefix vs. Postfix

|**Zápis**|**Název**|**Operace**|**Návratová hodnota**|
|---|---|---|---|
|`++counter`|**Prefix**|Zvýší hodnotu, **pak vrátí** novou hodnotu.|Nová hodnota|
|`counter++`|**Postfix**|Vrátí **původní hodnotu**, pak zvýší hodnotu.|Původní hodnota|

```javascript
let counter = 1;
let a = ++counter; // counter = 2, a = 2 (nová hodnota)

let b = counter--; // counter = 1, b = 2 (původní hodnota counter)
```

---

## 🔢 Priorita operátorů

- Operátory s **vyšší prioritou** se vyhodnocují dříve.
- Prioritu lze změnit pomocí **závorek** `()`.

```javascript
let x = 2 * 2 + 1; // Priorita: * (4) + (5). Výsledek: 5
let y = (2 + 2) * 1; // Závorky vynutí (2 + 2) = 4, pak 4 * 1. Výsledek: 4
```

---

## 🧠 Bitové operátory

Pracují s čísly na **binární úrovni** (reprezentace v 32 bitech).

| **Operátor** | **Název**       | **Popis**                                              | **Příklad**                                      |
| ------------ | --------------- | ------------------------------------------------------ | ------------------------------------------------ |
| `&`          | AND             | Bitový součin (výsledek 1 jen když oba bity jsou 1)    | `5 & 1` $\rightarrow$ `1` (`0101 & 0001 = 0001`) |
| \|           | OR              | Bitový součet (výsledek 1 když je alespoň jeden bit 1) | 5 \| 1 $\rightarrow$ 5 (`0101 \| 0001 = 0101`)   |
| `^`          | XOR             | Exkluzivní OR (výsledek 1 jen když jsou bity různé)    | `5 ^ 1` $\rightarrow$ `4` (`0101 ^ 0001 = 0100`) |
| `~`          | NOT             | Bitová negace (invertuje všechny bity)                 | `~5` $\rightarrow$ `-6`                          |
| `<<`         | Left shift      | Posun bitů doleva (ekvivalent násobení $2^N$)          | `5 << 1` $\rightarrow$ `10`                      |
| `>>`         | Right shift     | Posun bitů doprava (zachovává znaménko)                | `5 >> 1` $\rightarrow$ `2`                       |
| `>>>`        | Zero-fill shift | Posun doprava bez zachování znaménka (vždy doplní 0)   | `-5 >>> 1` $\rightarrow$ `2147483645`            |

---

## 🧮 Operátor čárky (`,`)

- Operátor čárky vyhodnotí **více výrazů** oddělených čárkou.
- **Vrátí hodnotu POUZE posledního výrazu**.
- Používá se zřídka (často v `for` smyčkách pro kombinování operací).

```javascript
// Hodnota "a" bude nastavena na hodnotu posledního výrazu, tj. 3 + 4 = 7
let a = (1 + 2, 3 + 4); // a = 7
```

---

## 🔎 Rozdíl v Prioritě a Zápisu

> [!example] `n *= 5 + 5` vs. `n = n * +5 + 5`
> 
> 1. **`n *= 5 + 5` (Zkrácené přiřazení)**
>     
>     - Nejprve se vyhodnotí pravá strana: `5 + 5` $\rightarrow$ `10`.
>         
>     - Celý výraz se přepíše na: `n = n * 10`.
>         
>     - _Příklad:_ `n = 2` $\rightarrow$ `n = 2 * 10` $\rightarrow$ **20**.
>         
> 2. **`n = n * +5 + 5` (Standardní operace)**
>     
>     - Vyhodnocuje se dle priority (`*` > `+`).
>         
>     - Nejprve: `n * +5` $\rightarrow$ `n * 5`.
>         
>     - Poté: `(n * 5) + 5`.
>         
>     - _Příklad:_ `n = 2` $\rightarrow$ `n = (2 * 5) + 5` $\rightarrow$ `10 + 5` $\rightarrow$ **15**.


```javascript
//Cvičení
let word = "a";
word += "h";
word += "o";
word += "j";
alert(word); //Vypíše "ahoj"

let a = 5;
a *= 4;
a /= 10;
a += 8;
a-= 3;
//Zkrácené výrazy
```