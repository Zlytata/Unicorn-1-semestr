
---

## 📌 Základní operátory

|**Kategorie**|**Operátor**|**Popis**|
|---|---|---|
|**Relační**|`>`|Větší než|
||`<`|Menší než|
||`>=`|Větší nebo rovno|
||`<=`|Menší nebo rovno|
|**Volná rovnost**|`==`|Rovnost (s **konverzí** typů)|
||`!=`|Nerovnost (s **konverzí** typů)|
|**Striktní rovnost**|`===`|**Striktní rovnost** (BEZ konverze typů)|
||`!==`|**Striktní nerovnost** (BEZ konverze typů)|

---

## 🔤 Porovnání řetězců

- Probíhá **lexikograficky** (abecedně), porovnáván je **Unicode** kód každého znaku, znak po znaku.
- Rozlišuje se **velikost písmen**: znaky s malými písmeny (`"a"`) mají vyšší kód než velká písmena (`"A"`).
    
    ```javascript
    "apple" > "pineapple"; // false (a < p)
    "a" > "A";             // true (nižší Unicode kód pro 'A')
    "2" > "12";            // true (Porovnává se první znak: "2" > "1")
    ```

---

## 🔄 Porovnání různých typů (`==`, `>`, `<`)

Při porovnávání různých datových typů **dochází k jejich konverzi** na číslo (_Number_), s výjimkou striktního porovnání a dvojice `null`/`undefined`.

- **Řetězec a Číslo:** Řetězec se převede na číslo.
    
    ```javascript
    "2" > 1;   // "2" -> 2. Výsledek: true
    "01" == 1; // "01" -> 1. Výsledek: true
    ```
    
- **Boolean:** Konvertuje se na číslo (`true` $\rightarrow$ `1`, `false` $\rightarrow$ `0`).
    
    ```javascript
    true == 1;  // true
    false == 0; // true
    ```
    

> [!warning] Příklad zvláštní konverze
> 
> ```javascript
> 0 == "0"; // true (Řetězec "0" se převede na číslo 0)
>  // Ale pozor na Boolean:
> Boolean("0"); // true ("0" je neprázdný řetězec, který je truthy)
> Boolean(0); // false (0 je falsy)
> ```

---

## 🟰 Striktní rovnost (`===` a `!==`)

Striktní operátory **vždy kontrolují typ**. Pokud jsou typy odlišné, výsledkem je bez další konverze `false`.

> [!tip] Doporučení
> 
> Pro zamezení nečekaného chování způsobeného konverzí se doporučuje používat === (striktní rovnost) a !== (striktní nerovnost).

```javascript
0 === false; // false (Různé typy: Number vs. Boolean)
0 == false;  // true (Volná rovnost: false -> 0)
```

---

## ⚠️ Speciální Hodnoty: `null` a `undefined`

Chování těchto hodnot je často matoucí kvůli odlišným pravidlům pro volnou rovnost (`==`) a relační operátory (`<`, `>`).

### Pravidla pro `==` (Volná rovnost)

1. **Speciální dvojice:** `null` a `undefined` jsou si navzájem volně rovny.
    
    ```javascript
    null == undefined; // true
    ```
    
2. **Žádné jiné rovnosti:** `null` ani `undefined` se volně **nerovnají žádné jiné hodnotě**, včetně `0`.
    
    ```javascript
    null == 0;      // false
    undefined == 0; // false
    ```

### Pravidla pro Relační operátory (`<`, `>`, $`\le`$, $`\ge`$)

Při porovnání pomocí relačních operátorů se hodnoty `null` a `undefined` snaží přetypovat na číslo.

|**Hodnota**|**Konverze na číslo**|**Chování**|
|---|---|---|
|**`null`**|$\rightarrow$ `0`|Porovnává se jako nula (0).|
|**`undefined`**|$\rightarrow$ `NaN`|Porovnávání s `NaN` je **vždy `false`**.|

> [!danger] Paradox s null
> 
> Vede k paradoxu, kdy platí následující:
> 
> 1. `null > 0` $\rightarrow$ `0 > 0` $\rightarrow$ **`false`**
>     
> 2. `null == 0` $\rightarrow$ Speciální pravidlo $\rightarrow$ **`false`**
>     
> 3. `null >= 0` $\rightarrow$ `0 >= 0` $\rightarrow$ **`true`**
>     
> 
> `null` tedy není větší než nula a nerovná se nule, ale zároveň je větší nebo roven nule.

### Chování `undefined`

Protože se `undefined` konvertuje na `NaN` (Not a Number), porovnávání selhává u všech relačních operátorů.

```javascript
undefined > 0;  // false (NaN > 0)
undefined < 0;  // false (NaN < 0)
undefined >= 0; // false (NaN >= 0)
```