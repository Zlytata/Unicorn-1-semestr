
---

Logické operátory jsou klíčové pro vyhodnocování podmínek. V JavaScriptu fungují nejen s booleany (`true`/`false`), ale vracejí i původní operandy (tzv. **Non-Boolean Return Values**).

---

## 🌟 Logické OR (`||`)

### 🔑 Chování (Non-Boolean Return)

Operátor `||` vyhodnocuje operandy zleva doprava a **vrací první _truthy_ hodnotu**, na kterou narazí.

- Pokud jsou všechny hodnoty **falsy**, vrací **poslední** z nich.

> [!example] Příklady OR (`||`)
> 
> ```javascript
> alert(1 || 0);               // 1 (první truthy)
> alert(null || "no comment"); // "no comment" (první truthy)
> alert(null || undefined || 0); // 0 (všechny falsy, vrací poslední)
> ```

### ⚡ Zkratové vyhodnocení (_Short-circuiting_)

Jakmile `||` najde první _truthy_ hodnotu, **zbytek výrazu se již nevyhodnocuje** (ignoruje se).

### 🛠️ Využití

1. **Získání první _truthy_ hodnoty** ze seznamu možností (např. nastavení priorit zobrazení dat).
2. **Nastavení výchozí hodnoty** (fallback):
    
    ```javascript
    let uzivatelskeJmeno = jmenoZDatabaze || "Host"; 
    ```
    

---

## 🚀 Logické AND (`&&`)

### 🔑 Chování (Non-Boolean Return)

Operátor `&&` vyhodnocuje operandy zleva doprava a **vrací první _falsy_ hodnotu**, na kterou narazí.

- Pokud jsou všechny hodnoty **truthy**, vrací **poslední** z nich.

> [!example] Příklady AND (`&&`)
> 
> ```javascript
> alert(1 && 0);             // 0 (první falsy)
> alert(1 && 5 && null && 3); // null (první falsy)
> alert("Hello" && "World"); // "World" (všechny truthy, vrací poslední)
> ```

### ⚡ Zkratové vyhodnocení (_Short-circuiting_)

Jakmile `&&` najde první _falsy_ hodnotu, **zbytek výrazu se ignoruje**.

### 🛠️ Využití

1. **Podmíněné spuštění kódu** (podmíněná operace):
    
    ```javascript
    // Kód se spustí POUZE, pokud je 'podminka' true/truthy.
    podminka && spustitFunkci(); 
    ```
    
2. **Kontrola všech povinných hodnot** (např. formuláře).

---

## 🔄 Logické NOT (`!`)

### 🔑 Chování

Unární operátor, který:

1. Převede operand na boolean (`true`/`false`).
2. Vrátí **opačnou** logickou hodnotu.

> [!example] Příklady NOT (`!`)
> 
> ```javascript
> alert(!true); // false
> alert(!0);    // true (0 je falsy, negace je true)
> alert(!"");   // true ("" je falsy, negace je true)
> ```

### ❗ Dvojité NOT (`!!`)

Dvojitá negace se používá jako **nejkratší způsob pro explicitní převod jakékoliv hodnoty na její čistou boolean ekvivalent**. Má stejný výsledek jako `Boolean(hodnota)`.

```javascript
alert(!!"non-empty string"); // true
alert(!!null);              // false
```

---

## 📊 Priorita a Pořadí Vyhodnocení

Při kombinaci logických operátorů se uplatňuje následující priorita (od nejvyšší k nejnižší):

1. **`!` (NOT):** Nejvyšší priorita.
2. **`&&` (AND):** Střední priorita.
3. **`||` (OR):** Nejnižší priorita.

> [!tip] Používejte Závorky! Pokud si nejste jisti pořadím vyhodnocení, nebo chcete kód zpřehlednit, **vždy použijte závorky `()`**.
> 
> ```javascript
> // Vždy používejte závorky, i když je pořadí dáno prioritou:
> (a && b) || (c && d)
> // Je mnohem čitelnější než: a && b || c && d
> ```

```javascript
let isOpen = false;
isOpen = !!isOpen; //isOpen = false 

const isValue = 0 || "ahoj" && "hodnota" // true, protože podmínka za OR je splněna
```