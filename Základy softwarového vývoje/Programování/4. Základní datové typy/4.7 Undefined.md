
---

**Undefined** (_Undefined_) je primitivní datový typ, který má pouze jednu hodnotu: **`undefined`**. Tato hodnota primárně signalizuje, že **proměnné byla deklarována, ale nebyla jí přiřazena žádná hodnota**.

### 1. Klíčové Vlastnosti a Původ

- **Původ (Nedostatek Hodnoty):** `undefined` je hodnota, kterou jazyk automaticky přiřazuje, když zjistí, že hodnota chybí. Je to **výchozí stav** (_default state_).
    
- **Vzniká automaticky v těchto případech:**
    
    - **Deklarovaná proměnná bez přiřazení:**
        
        ```javascript
        let promennaBezHodnoty; // Hodnota je automaticky 'undefined'
        ```
        
    - **Přístup k neexistující vlastnosti** objektu (_Object property_).
        
    - **Parametr funkce**, pro který nebyl dodán žádný argument.
        
    - **Návratová hodnota** funkce, která nemá explicitní `return` příkaz (vrací `undefined`).
        
- **Datový Typ:** Stejně jako u `null` se jedná o primitivní typ, ale na rozdíl od něj je `typeof undefined` konzistentní:
    
    ```javascript
    typeof undefined; // Vrací "undefined"
    ```
    

---

### 2. Rozdíl mezi `null` a `undefined`

Toto je jeden z nejčastějších nejasností v JavaScriptu.

|**Kritérium**|**undefined**|**null**|
|---|---|---|
|**Účel**|**Chybějící hodnota** (_Missing value_) – přiřazuje **systém** automaticky.|**Prázdná hodnota** (_Empty value_) – přiřazuje **vývojář** záměrně.|
|**Příklad vzniku**|`let x;`|`let x = null;`|
|**`typeof`**|`"undefined"`|`"object"` (Historická chyba)|

### 3. Porovnání s Jinými Hodnotami

#### A. Přísné Porovnání (`===`)

`undefined` se přísně rovná pouze sobě.

|**Porovnání**|**Výsledek**|**Důvod**|
|---|---|---|
|`undefined === undefined`|`true`|Stejná hodnota i typ.|
|`undefined === null`|`false`|Hodnoty jsou si podobné, ale **nejsou stejného typu**.|
|`undefined === 0`|`false`|Různé hodnoty a typy.|
|`undefined === NaN`|`false`|Různé hodnoty a typy.|

#### B. Volné Porovnání (`==`)

Díky speciálnímu pravidlu volného porovnávání v JavaScriptu se `undefined` chová stejně jako `null`.

|**Porovnání**|**Výsledek**|**Důvod**|
|---|---|---|
|**`undefined == null`**|`true`|Speciální pravidlo JavaScriptu je považuje za ekvivalentní v kontextu "absence hodnoty".|
|`undefined == 0`|`false`|Ostatní hodnoty vyžadují přetypování (na `NaN` nebo `0`), které ve většině případů selže.|

#### C. Chování s Čísly (Relační Operátory)

Při použití relačních operátorů (`<`, `>`, `<=`, `>=`) proti číslům (včetně 0) se `undefined` snaží přetypovat na číslo, což vede k **`NaN`**.

|**Porovnání**|**Přetypování**|**Výsledek**|**Důvod**|
|---|---|---|---|
|`undefined > 0`|`NaN > 0`|`false`|**NaN** nikdy není větší než cokoliv.|
|`undefined < 0`|`NaN < 0`|`false`|**NaN** nikdy není menší než cokoliv.|
|`undefined == 0`|`undefined` není číslo.|`false`|Volné porovnání dává `false`, **nelze** je převést na `0` a porovnat.|
|`undefined >= 0`|`NaN >= 0`|`false`|**NaN** není ani větší, ani menší, ani rovno.|

> 💡 **Doporučení pro kontrolu:** Pro kontrolu, zda je hodnota buď `null`, nebo `undefined`, je nejlepší použít volné porovnání (`hodnota == null`) nebo explicitně (`hodnota === null || hodnota === undefined`).