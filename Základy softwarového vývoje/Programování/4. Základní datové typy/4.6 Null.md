
---

**Null** (_Null_) je primitivní datový typ, který má pouze jedinou hodnotu: **`null`**. Hodnota `null` představuje záměrné **nastavení proměnné na prázdnou/nulovou hodnotu**.

### 1. Klíčové Vlastnosti

- **Záměrná Absence:** Na rozdíl od `undefined` (která značí, že proměnná nemá _ještě_ přiřazenu žádnou hodnotu), `null` je hodnota, kterou vývojář proměnné explicitně přiřadí, aby vyjádřil, že **záměrně neobsahuje žádný objekt nebo smysluplnou hodnotu**.
    
- **Historická Chyba:** Když v JavaScriptu použijete operátor `typeof` na `null`, vrátí `"object"`. Jedná se o **historickou chybu** (_bug_) v návrhu jazyka, která byla uznána, ale kvůli zpětné kompatibilitě nikdy nebyla opravena. Přesto je `null` primitivní typ.
    
    ```javascript
    typeof null; // Vrací "object" (historická chyba)
    ```
    

---

### 2. Porovnání (_Null Comparisons_)

Chování `null` při porovnávání je často matoucí kvůli tomu, jak probíhá implicitní přetypování (_type coercion_) v JavaScriptu.

#### A. Přísné Porovnání (`===`)

Přísné porovnání kontroluje **hodnotu** i **datový typ**. Zde nedochází k žádnému přetypování, a proto jsou výsledky intuitivní.

|**Porovnání**|**Výsledek**|**Důvod**|
|---|---|---|
|`null === null`|`true`|Hodnoty i typy jsou shodné.|
|`null === undefined`|`false`|Hodnoty i když jsou si podobné, **nejsou stejného typu**.|
|`null === 0`|`false`|Různé hodnoty i typy.|
|`null === NaN`|`false`|Různé hodnoty i typy.|

#### B. Volné Porovnání (`==`)

Volné porovnání kontroluje pouze hodnotu, ale umožňuje **implicitní přetypování** datových typů.

|**Porovnání**|**Výsledek**|**Důvod**|
|---|---|---|
|**`null == undefined`**|`true`|Je to **jediný** případ, kdy se tyto dvě hodnoty rovnají při volném porovnání. Jde o speciální pravidlo v jazyce, které je považuje za ekvivalentní v kontextu "absence hodnoty".|
|**`null == 0`**|`false`|Porovnávací algoritmus se snaží `null` převést na číslo, ale používá speciální pravidlo, které **vrací `false`**, na rozdíl od relačních operátorů.|
|**`null == NaN`**|`false`|Žádná hodnota se nikdy **nerovná `NaN`** (ani `NaN == NaN` je `false`).|

#### C. Relační Operátory (`<`, `>`, `<=`, `>=`)

Při porovnání `null` s čísly pomocí relačních operátorů se `null` **přetypuje na číslo `0`**. To vede k často překvapivým výsledkům.

|**Porovnání**|**Přetypování**|**Výsledek**|**Důvod**|
|---|---|---|---|
|`null > 0`|`0 > 0`|`false`|Nula není větší než nula.|
|`null < 0`|`0 < 0`|`false`|Nula není menší než nula.|
|**`null >= 0`**|`0 >= 0`|`true`|**Nula je větší nebo rovna nule.**|

> 💡 **Doporučení:** Aby se vývojář vyhnul těmto matoucím pravidlům, **vždy** by měl pro kontrolu rovnosti používat **přísné porovnání** (`===`).