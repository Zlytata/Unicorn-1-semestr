
---

## ✨ Co je Function Expression

Function Expression (FE) je funkce, která je vytvořena **uvnitř výrazu** a je často **přiřazena do proměnné**.

> [!example] Příklad 
> 
> 
> ```javascript
> let sayHi = function () {
>	alert("Hello");
>};
> ```

- **Anonymní funkce:** Název funkce může být v FE vynechán.
- **Význam:** „Vytvoř funkci a ulož ji do proměnné `sayHi`.“

---

## 📌 Funkce jako hodnota

Funkce v JavaScriptu jsou **hodnoty první třídy** – lze s nimi zacházet jako s jakýmkoli jiným datovým typem (`string`, `number`):

- uložit do proměnné,
- **kopírovat** do jiné proměnné,
- předat jako **argument** jiné funkci (callback),
- **vrátit** z funkce.

>[!example] **Příklad kopírování:**
>
>```javascript
>function sayHi() {
>	alert("Hello");
>}
>
>let func = sayHi; // Funkce je zkopírována
>func(); // Hello
>sayHi(); // Hello
>```

---

## ❓ Proč má Function Expression středník

Function Expression je součástí **přiřazovacího příkazu** (assignment statement):

```javascript
let sayHi = function() { ... }; 
```

Středník (`;`) **neukončuje syntaxi funkce**, ale **celý příkaz přiřazení**. U samostatné Function Declaration není středník potřeba.

---

## 🔄 Callback funkce

Callback je funkce, která je **předána jako argument** jiné funkci s očekáváním, že bude vykonána později.

- Anonymní funkce se často používají jako callbacky pro jednorázové použití.

>[!example] **Příklad Callbacku:**
>
>```javascript
>function ask(question, yes, no) {
>if (confirm(question)) yes();
>else no();
>}
>
>ask(
>"Do you agree?",
>function () { // Anonymní callback pro 'yes'
>	alert("You agreed.");
>},
>function () { // Anonymní callback pro 'no'
>	alert("You canceled.");
>},
>);
>```

---

## 🍴 Currying (Karáování)

Currying je pokročilá technika, při které se funkce s více argumenty (např. `f(a, b, c)`) transformuje na **sekvenci vnořených funkcí**, z nichž každá přijímá pouze jeden argument (např. `f(a)(b)(c)`).

- **Princip:** Funkce při zavolání s prvním argumentem **nevrátí výsledek**, ale **novou funkci**, která čeká na další argument.
- **Výhoda:** Umožňuje vytvářet snadno znovupoužitelné, **parciálně aplikované** funkce.

>[!example]  **Příklad (Currying):**
>```javascript
>function multiply(a) {
>return function(b) {
>return a * b; // Uzavírá se nad proměnnou 'a'
>};
>}
>
>let double = multiply(2);  // double je funkce, která má 'a' = 2
>let triple = multiply(3);  // triple je funkce, která má 'a' = 3
>
>alert(double(5));  // Ukáže 10
>alert(triple(5));  // Ukáže 15
>```

---

## ⚖️ Function Declaration (FD) vs Function Expression (FE)

| Vlastnost                | Function Declaration (`function f() {}`) | Function Expression (`let f = function() {}`) |
| ------------------------ | ---------------------------------------- | --------------------------------------------- |
| **Kdy se vytvoří**       | **Před** spuštěním kódu (Hoisting)       | **Až při** dosažení řádku kódu                |
| **Viditelnost**          | V celém bloku, kde je deklarována.       | Od místa vytvoření dál.                       |
| **Volání před definicí** | ✅ **Ano** (díky Hoisting)                | ❌ **Ne** (proměnná ještě neexistuje)          |

### **Blokový rozsah FD**

Ve **strict mode** jsou deklarace funkcí uvnitř bloku (`if`, `for`) viditelné pouze v daném bloku, což vede k chybám, pokud se je snažíte volat mimo něj.

- **Řešení:** Vždy použít **Function Expression** s proměnnou deklarovanou mimo blok (`let welcome;`).

---

## 🎯 Kdy použít kterou

- **Function Declaration (FD):**
    - **Preferovaná volba** pro běžné deklarace.
    - Zlepšuje **čitelnost**, protože čtenář vidí implementaci ihned.
    - Ideální pro hlavní funkce.
    
- **Function Expression (FE):**
    - Pro **jednorázové funkce** (callbacky, IIFE).
    - Když potřebujeme **podmíněnou deklaraci** (`if/else`).
    - Když je funkce navázána na proměnnou, kterou chceme chránit před Hoistingem.

---

## 📋 Shrnutí

- Funkce jsou **hodnoty** – lze je ukládat, kopírovat, předávat (callbacky).
- **FD** – Vytvořena před spuštěním kódu (Hoisting), lze volat dříve.
- **FE** – Vytvořena až při dosažení kódu, viditelná od té chvíle.
- Používej **Function Declaration**, pokud není důvod pro opak (např. callback nebo podmíněná logika).