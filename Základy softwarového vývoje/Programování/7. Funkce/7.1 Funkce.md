
---

## ✨ Jak funkce funguje

- **Deklarace:** Pomocí klíčového slova `function` definujeme název, parametry a tělo.
- **Volání:** Funkci spustíme jejím názvem následovaným závorkami, např. `showMessage()`.
- **Opakovatelnost:** Umožňuje vyhnout se duplicitnímu kódu (DRY).

```javascript
function showMessage() {
    alert("Hello everyone!");
}
showMessage();
```

---

## 🎯 Scope (Rozsah platnosti) a proměnné

- **Lokální proměnné:** Proměnné uvnitř funkce (`{}`) jsou dostupné **pouze** v jejím rámci.
- **Vnější proměnné:** Funkce může číst i měnit proměnné definované mimo ni.
- **Stínování (Shadowing):** Pokud má funkce lokální proměnnou se stejným názvem jako vnější, vždy použije tu **lokální**.

---

## 📦 Parametry a argumenty

- **Parametry:** Proměnné definované v záhlaví funkce (očekávané vstupy).
- **Argumenty:** Konkrétní hodnoty, které jsou předány při volání.

```javascript
function showMessage(from, text) { 
    alert(from + ": " + text);
}
showMessage("Ann", "Hello!"); 
```

### 💧 ...Rest parametry (zbytek)

Rest parametr (`...nazev`) umožňuje funkci přijmout **neurčitý počet argumentů** a sesbírat je do **pole**.

> **Pravidlo:** Rest parametr musí být _vždy_ posledním parametrem ve funkci.

```javascript
function sumAll(...cisla) {
    let soucet = 0;
    for (let cislo of cisla) {
        soucet += cislo;
    }
    return soucet;
}

alert(sumAll(1, 2, 3)); // Ukáže 6
```

---

## 🧩 Výchozí hodnoty (Default values)

- Pokud není argument předán, parametr má hodnotu `undefined`.
- Výchozí hodnotu nastavíme pomocí operátoru `=`.

```javascript
function showMessage(from, text = "no text given") {
    alert(from + ": " + text);
}
```

- **Alternativy:** `if (text === undefined)`, `text = text || 'default'`, nebo moderně `text ?? 'default'`.

---

## 🔁 Vrácení hodnoty (`return`)

- `return` slouží k **vrácení výsledku** a **okamžitému ukončení** funkce.

```javascript
function sum(a, b) {
    return a + b;
}
let result = sum(1, 2); 
```

> 💡 **Poznámka:** Pokud není `return`, funkce vrací `undefined`.

---

## 🧠 Pojmenování a modularita

### **Pojmenování**

- Název by měl být **sloveso**, které jasně popisuje akci.
- **Prefixy:** `get`, `calc`, `create`, `check`, `show`.

### **Jedna funkce = jedna akce**

- Funkce by měla dělat **pouze jednu věc** (Single Responsibility Principle).
- Složité úkoly rozdělte na menší, znovupoužitelné funkce.

---

## 🧪 Příklad modularity: test prvočísel

```javascript
// Kontroluje JEDNU věc: zda je prvočíslo
function isPrime(n) {
    for (let i = 2; i < n; i++) {
        if (n % i === 0) return false;
    }
    return true;
}

// Dělá JEDNU věc: vypisuje prvočísla pomocí isPrime
function showPrimes(n) {
    for (let i = 2; i < n; i++) {
        if (isPrime(i)) alert(i);
    }
}
```

---

# 📋 Závěrečné shrnutí

- **Deklarace:** `function name(params) { ... }`
- **Rest parametry:** `...jmeno` sesbírá zbytek argumentů do pole.
- **Vrácení hodnoty:** Pomocí `return`, jinak `undefined`.
- **Názvy funkcí:** Slovesné a popisné.
- **Modularita:** Funkce zvyšují čitelnost a strukturu kódu.