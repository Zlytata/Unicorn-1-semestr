# 📚 Souhrn informací z Algebraických výrazů

Tento dokument shrnuje základní pojmy, pravidla a vlastnosti algebraických výrazů, zlomků, mocnin, odmocnin a polynomů.

---

## I. Algebraické Výrazy a Podmínky

* **Definice:** Algebraický výraz je každý matematický zápis, který je tvořen z **konstant** (konkrétní čísla) a **proměnných** (libovolná písmena zastupující čísla), mezi nimiž jsou pomocí algebraických operací ($+$, $-$, $\cdot$, $\div$, $\sqrt{}$, $a^n$) a závorek tvořeny smysluplné vztahy.
* **Podmínky:** Požadavek matematického smyslu výrazu vede k nutnosti stanovit **podmínky**. Typicky se jedná o vyloučení nulou a dodržení definičních oborů funkcí ve výrazu (např. výraz $\sqrt{x-1}$ je v $\mathbb{R}$ definován jen pro $x \ge 1$).

---

## II. 1. Zlomky

* **Důležitá Definice:** **Zlomek** (lomený výraz) označuje podíl dvou výrazů, $\frac{a}{b}$. **$a$** nazýváme **čitatel** a **$b$** **jmenovatel**.
* **Podmínka:** Dělení nulou nemá smysl, proto musí být **jmenovatel** vždy **nenulový** ($b \ne 0$).

### Pravidla pro počítání se zlomky

* **Sčítání a odčítání zlomků:**
    $$\frac{a}{b} \pm \frac{c}{d} = \frac{a \cdot d \pm c \cdot b}{p}$$
    kde $p$ je nějaký společný násobek $b, d$ (nejoptimálnější volbou $p$ je **nejmenší společný násobek**).
* **Násobení zlomků:** Násobíme čitatel s čitatelem a jmenovatel se jmenovatelem.
    $$\frac{a}{b} \cdot \frac{c}{d} = \frac{a \cdot c}{b \cdot d}, \quad \text{kde } b, d \ne 0.$$
* **Dělení zlomků:** Dělit zlomkem znamená násobit jeho převrácenou hodnotou.
    $$\frac{a}{b} : \frac{c}{d} = \frac{a}{b} \cdot \frac{d}{c} = \frac{a \cdot d}{b \cdot c}, \quad \text{kde } b, c, d \ne 0.$$
* **Krácení zlomků:** Čitatel i jmenovatel lze vydělit libovolným nenulovým číslem, aniž by se změnila jeho hodnota.
    $$\frac{a \cdot c}{b \cdot c} = \frac{a}{b}, \quad \text{kde } b, c \ne 0.$$
* **Rozšiřování zlomků:** Je v podstatě opačný proces ke krácení - hodnota zlomku se nezmění, vynásobíme-li čitatele i jmenovatele stejným nenulovým číslem.
    $$\frac{a}{b} = \frac{a \cdot c}{b \cdot c}, \quad \text{kde } b, c \ne 0.$$

---

## III. 2. Mocniny a Odmocniny

### Mocniny

* **Důležitá Definice:** **Mocninou** rozumíme zkrácený zápis pro opakované násobení:
    $$a^r = \underbrace{a \cdot a \cdot \dots \cdot a}_{r \text{-krát}}, \quad \text{kde } a, r \in \mathbb{R}.$$
    $a$ je **základ mocniny** (mocněnec) a $r$ je **exponent** (mocnitel).

### Vlastnosti počítání s mocninami (pro $a, b \in \mathbb{R}$, $r, s \in \mathbb{N}$)

1.  $$a^r \cdot a^s = a^{r+s}$$
2.  $$(a \cdot b)^r = a^r \cdot b^r$$
3.  $$\frac{a^r}{a^s} = a^{r-s} \quad \text{pro } a \ne 0, r > s$$
4.  $$a^0 = 1 \quad \text{pro } a \ne 0$$
5.  $$\left( \frac{a}{b} \right)^r = \frac{a^r}{b^r} \quad \text{pro } b \ne 0$$
6.  $$(a^r)^s = a^{r \cdot s}$$
    * **Poznámka:** Speciálně $a^{-r} = \frac{1}{a^r}$ a $a^{1/r} = \sqrt[r]{a}$.

### Odmocniny

* **Důležitá Definice:** **$r$-tou odmocninou** z čísla $a$ rozumíme číslo $x$, jehož $r$-tá mocnina je číslo $a$.
    $$\sqrt[r]{a} = x \iff x^r = a$$

### Vlastnosti počítání s odmocninami (pro $a, b \in \mathbb{R}$, $r, s \in \mathbb{N}$, kde odmocniny mají smysl)

1.  $$\sqrt[r]{a^r} = a$$
2.  $$\sqrt[r]{a \cdot b} = \sqrt[r]{a} \cdot \sqrt[r]{b}$$
3.  $$\sqrt[r]{\frac{a}{b}} = \frac{\sqrt[r]{a}}{\sqrt[r]{b}} \quad \text{pro } b \ne 0$$
4.  $$\sqrt[r]{\sqrt[s]{a}} = \sqrt[r \cdot s]{a}$$
5.  $$(\sqrt[r]{a})^s = \sqrt[r]{a^s}$$
    * **Pozor:** Obecně platí $a^r + b^r \ne (a+b)^r$.

---

## IV. 3. Polynomy

* **Důležitá Definice:** **Polynom $n$-tého stupně** je výraz tvaru $a_n x^n + a_{n-1} x^{n-1} + \dots + a_1 x + a_0$, kde $a_i \in \mathbb{R}$ a $a_n \ne 0$. Číslo $n$ se nazývá **stupeň polynomu**.
    Zkrácený zápis sumy: $$P_n(x) = \sum_{i=0}^{n} a_i x^i$$

### Speciální případy

* $n=0$: $P_0(x) = a_0$ (konstanta)
* $n=1$: $P_1(x) = a_0 + a_1 x$ (lineární dvojčlen)
* $n=2$: $P_2(x) = a_0 + a_1 x + a_2 x^2$ (kvadratický trojčlen)
* $n=3$: $P_3(x) = a_0 + a_1 x + a_2 x^2 + a_3 x^3$ (kubický čtyřčlen)

### Operace s polynomy

* **Sčítání a odčítání:** Sčítáme a odčítáme pouze členy téhož stupně. Např. $P(x) + Q(x) = (a_n+b_n)x^n + \dots + (a_0+b_0)$.
* **Násobení:** Provádí se využitím distribučního zákona $a \cdot (b+c) = a \cdot b + a \cdot c$.
    $$P(x) \cdot Q(x) = \left( \sum_{i=0}^{m} a_i x^i \right) \cdot \left( \sum_{j=0}^{n} b_j x^j \right) = \sum_{i=0}^{m} \sum_{j=0}^{n} (a_i b_j) x^{i+j}$$

### Kořeny a Rozklad polynomů

* **Důležitá Definice:** Číslo $\alpha$ se nazývá **kořen polynomu** $P(x)$, jestliže platí **$P(\alpha) = 0$**.
* **Základní věta algebry:** Každý polynom stupně $n > 0$ s reálnými koeficienty má alespoň jeden kořen v komplexních číslech ($\mathbb{C}$).
* **Důsledek (Rozklad na kořenové činitele):** Každý monický polynom (polynom stupně $n$ pro nějž je $a_n = 1$) lze zapsat jako součin tzv. **kořenových činitelů**:
    $$P(x) = (x - x_1) (x - x_2) \dots (x - x_n)$$
    kde $x_1, x_2, \dots, x_n$ jsou kořeny polynomu $P$.
* **Obecný Rozklad:** Polynom $P(x)$ lze obecně rozložit pomocí součinu dvou polynomů nižšího stupně a zbytku:
    $$P(x) = Q(x) R(x) + S(x)$$
    kde $P, Q, R, S$ jsou polynomy a stupeň $S(x)$ je menší než stupeň $Q(x)$. Pokud $S(x) = 0$, říkáme, že polynom $P(x)$ je **dělitelný** polynomy $Q(x)$ a $R(x)$.

### Metody rozkladu polynomu

1.  **Dělením** (tomu se pokoušíme vyhnout).
2.  Nalezením kořenů a rozpisem polynomu jako součinu kořenových činitelů.
3.  Speciální postupy - např. **využití vzorečků** nebo **doplnění na čtverec**.

#### Doplnění na čtverec (Kvadratický trojčlen)

Cílem je vyjádřit kvadratický trojčlen $a x^2 + b x + c$ jako součet (nebo rozdíl) čtverce dvojčlenu a zbytku: $a(x-b)^2 + c'$.

Obecně:
$$a x^2 + b x + c = a \left( x + \frac{b}{2a} \right)^2 + \left( c - \frac{b^2}{4a} \right)$$

**Rozklad na kořenové činitele pro kvadratický trojčlen:**
Pomocí vzorce $A^2 - B^2 = (A-B)(A+B)$ z předchozího tvaru (pokud je $c - \frac{b^2}{4a} \le 0$):
$$a x^2 + b x + c = a \left( x - \frac{-b + \sqrt{b^2 - 4ac}}{2a} \right) \left( x - \frac{-b - \sqrt{b^2 - 4ac}}{2a} \right)$$

Kořeny polynomu $a x^2 + b x + c$ jsou tedy obecné výrazy:
$$x_{1, 2} = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$$
což je známý vzorec pro výpočet kořenů kvadratického polynomu, kde $D = b^2 - 4ac$ je **diskriminant**.

---

## V. 4. Užitečné Vzorečky a Algoritmy

### Vzorce pro rozklad na součin (zkrácené násobení)

* $$(a \pm b)^2 = a^2 \pm 2ab + b^2$$
* $$(a + b)(a - b) = a^2 - b^2$$
* $$(a \pm b)^3 = a^3 \pm 3a^2b + 3ab^2 \pm b^3$$
* $$(a + b)(a^2 - ab + b^2) = a^3 + b^3$$
* $$(a - b)(a^2 + ab + b^2) = a^3 - b^3$$

### Binomická věta

* $$(a + b)^n = \sum_{i=0}^{n} \binom{n}{i} a^{n-i} b^i$$
    kde kombinační číslo $\binom{n}{i}$ je definováno jako:
    $$\binom{n}{i} = \frac{n!}{i! (n-i)!}$$


---